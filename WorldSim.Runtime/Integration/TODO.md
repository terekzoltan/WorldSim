# Integration TODO

- Add explicit Java-to-C# tech ID mapping table for cases where planner tech IDs do not directly match `Tech/technologies.json`.
- Persist patch dedupe state (`AppliedOpIds`) once world save/load pipeline is in place.
