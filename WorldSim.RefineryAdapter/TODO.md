# Track D TODO

- Add explicit Java-to-C# tech ID mapping configuration layer (`ITechIdMapper`) when mapping work starts.
- Persist dedupe state (`AppliedOpIds`) once save/load lifecycle is implemented in runtime.
