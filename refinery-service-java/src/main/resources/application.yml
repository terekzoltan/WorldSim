server:
  port: ${REFINERY_SERVICE_PORT:8091}

app:
  version: ${APP_VERSION:0.1.0}

planner:
  mode: ${PLANNER_MODE:mock}
  llm:
    enabled: ${PLANNER_LLM_ENABLED:false}

logging:
  pattern:
    level: "%5p [requestId:%X{requestId:-} goal:%X{goal:-} seed:%X{seed:-} tick:%X{tick:-}]"
